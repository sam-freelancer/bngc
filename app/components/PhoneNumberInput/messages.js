/*
 * PhoneNumberInput Messages
 *
 * This contains all the text for the PhoneNumberInput.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.screens.PhoneNumberInput';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'PhoneNumberInput',
  },
});
